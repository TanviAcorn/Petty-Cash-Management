import React from 'react';
import { Box, Typography } from '@mui/material';

const Companies = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Companies</Typography>
      <Typography>Manage company information</Typography>
    </Box>
  );
};

export default Companies;
