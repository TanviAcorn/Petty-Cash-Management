import React from 'react';
import { Box, Typography } from '@mui/material';

const Rejected = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Rejected Requests</Typography>
      <Typography>List of all rejected requests</Typography>
    </Box>
  );
};

export default Rejected;
