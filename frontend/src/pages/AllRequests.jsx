import React from 'react';
import { Box, Typography } from '@mui/material';

const AllRequests = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>All Requests</Typography>
      <Typography>List of all petty cash requests</Typography>
    </Box>
  );
};

export default AllRequests;
